# === NEAR Testnet Configuration ===
# Used when NEAR_ENV=testnet

# Environment
NEAR_ENV=testnet

# Master Account (needs to be funded with NEAR tokens)
MASTER_ACCOUNT=your-account.testnet
MASTER_ACCOUNT_PRIVATE_KEY=ed25519:your-private-key-here

# FT Contract (will be deployed)
FT_CONTRACT=your-ft-contract.testnet

# RPC Configuration (NEAR official for @eclipseeer/near-api-ts compatibility)
NODE_URL=https://rpc.testnet.near.org
RPC_URLS=https://rpc.testnet.near.org

# === NEAR Sandbox Configuration ===
# Used when NEAR_ENV=sandbox (for local testing)

# Environment
# NEAR_ENV=sandbox

# Sandbox uses default accounts (no funding needed)
# MASTER_ACCOUNT=test.near
# MASTER_ACCOUNT_PRIVATE_KEY=ed25519:your-sandbox-private-key-here
# FT_CONTRACT=ft.test.near
# RECEIVER_ID=user.test.near
# NODE_URL=http://127.0.0.1:3030

# Optional Service Settings
SKIP_STORAGE_CHECK=false
STORAGE_MIN_DEPOSIT=1250000000000000000000
CONCURRENCY_LIMIT=1000
WORKER_COUNT=6
WAIT_UNTIL=Included